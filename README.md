# pefile-extract-cert
This sample script demonstrates how to extract a digital signature certificate from a PE (Portable Executable) file using a manual approach, bypassing the built-in IMAGE_DIRECTORY_ENTRY_SECURITY provided by pefile lib
